<h3>以下の内容で、投稿する。</h3>

<%= form_with(model: @picture, local: true,url: confirm_pictures_path) do |form| %>
  <%= form.hidden_field :content %>
  <%= form.hidden_field :picture %>

  <p>タイトル:<%= @picture.content %></p>
  <p>本文:<%= @picture.picture %></p>
  <%= form.submit  "登録する" %>
<% end %>

<%= link_to '戻る', :back %>