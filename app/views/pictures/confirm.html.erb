<h3>以下の内容で、投稿する。</h3>

<%= form_with(model: @picture, local: true,url: choose_new_or_edit) do |form| %>
  <%= form.hidden_field :content %>
  <%= form.hidden_field :picture %>

  <p>本文:<%= @picture.content %></p>
  <p>写真:<%= @picture.picture %></p>
  <%= form.submit  "登録する" %>
<% end %>

<%= link_to '戻る', :back %>